# All pure-Python wheels — no C++ build dependencies, works on Windows and Linux.
# Install in a virtual environment to avoid conflicts with system packages:
#   python -m venv .venv
#   .venv\Scripts\activate    (Windows)
#   source .venv/bin/activate (Linux/macOS)
#   pip install -r requirements.txt

xarray>=2024.6.0
numpy>=1.26.4
netCDF4>=1.7.2
Pillow>=10.4.0
matplotlib>=3.9.2
requests>=2.32.3
